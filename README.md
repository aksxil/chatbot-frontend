💬 chatbot-ui (React App)
Description

A React (Vite) based chatbot UI that loads inside an iframe and communicates with the backend API.

Tech Stack

React

Vite

Axios

🔧 Setup Instructions (Chatbot UI)
cd chatbot-ui
npm install
npm run dev


The UI runs on:

http://localhost:5173

🔌 Backend Connection

The UI sends messages to:

/api/chat


Backend URL can be configured in:

src/api.js
